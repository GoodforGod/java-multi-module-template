pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
apply from: "dependencies.gradle"

rootProject.name = artifactRootId

include "module-one"
include "module-two"

